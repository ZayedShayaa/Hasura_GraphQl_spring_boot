mutation InsertNewStudent($name: String!, $email: String!, $age: Int!) {
  insert_student_one(object: {
    name: $name,
    email: $email,
    age: $age
  }) {
    id
    name
    email
    age
  }
}
